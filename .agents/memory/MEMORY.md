- [Wrapping cards in a stacking-context component](stacking-context-card-wrap.md) — absolute dropdowns inside a card break when the card is wrapped in a component using isolation/transform; raise the whole card's z-index while the overlay is open.
- [GSAP scroll-reveal integration](gsap-scrollreveal-integration.md) — scrub reveals look "broken" when already in view; use timed timeline + toggleActions; reuse on cards via a ref-only hook (no wrapper) + forwardRef; call ScrollTrigger.refresh() after client-side filtering or cards stick at opacity 0.
- [Card bottom-fade blur removed](gradual-blur-cards.md) — user disliked the GradualBlur bottom-fade on cards; CardBlur + GradualBlur deleted. Do NOT reintroduce a card bottom-fade/backdrop-filter strip unless asked. (Reusable technique notes kept inside.)
- [TiltedCard wrapper constraints](tilt-card-wrapper.md) — generic mouse-tilt wrapper: flat transform (no preserve-3d/translateZ) to keep backdrop-filter; lazy useState pointer gate to avoid div→motion.div remount flash; dual height:100% for grid-vs-stack; disable tilt + elevate wrapper while an in-card dropdown is open.
- [WebGL global background (three.js Beams)](webgl-background.md) — pre-flight supportsWebGL() before mounting <Canvas> or the dev error-modal fires even with an error boundary; theme colors as reactive props (no Canvas remount); mat.lights post-construct; screenshot tool has no WebGL.
- [Liquid-glass surfaces](glass-surfaces.md) — "glass everywhere" = translucent --card + backdrop-filter via .glass-panel/.glass-popover + BorderGlow --card-bg, NOT raw React Bits GlassSurface (force-centers, fixed size, clips dropdowns); rewrite its light-dark()/prefers-color-scheme to .dark class.
- [runTest Clerk block](runtest-clerk-block.md) — a prior blocked test session (Clerk→Google OAuth wall) permanently blocks runTest; pass testClerkAuth:true on the FIRST attempt or fall back to architect+curl+screenshots.
- [Clerk API 401s](clerk-orval-bearer.md) — signed-in user but all /api calls 401: the Orval client needs setAuthTokenGetter wired to Clerk getToken(); cookies alone don't auth the API. Verify with a signed-in 200, not just unauth 401s.
- [OpenAI proxy web search](openai-proxy-web-search.md) — Responses API web_search works via the AI Integrations proxy (undocumented in skill); with json_schema output, url_citation annotations are empty — put sources in the JSON schema instead.
